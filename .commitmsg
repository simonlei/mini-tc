feat: Shift+Delete 永久删除（绕过回收站）

- 后端新增 delete_permanently 命令：文件/目录直接抹除，目录走
  remove_dir_all；删除前递归清除只读属性（Windows 上只读文件会中断
  删除），符号链接只删链接本身不穿透目标；返回与 delete_to_trash 相同
  的 DeleteError 结构，权限失败时前端可回退 UAC 提权删除
- 快捷键注册表新增 list.deletePermanent（文件列表作用域），默认
  Shift+Delete / Ctrl+Shift+Backspace / Shift+Meta+Backspace，可在
  配置 → 快捷键设置中自定义
- FileList: Shift+Delete 走 deleteSelected({ permanent: true })
- FilePanel: 永久删除前弹确认框（列出条目，Enter 确认 / Esc 取消），
  确认后调 deletePermanently；路径不存在时不再无谓地弹 UAC
- README 补充删除 / 永久删除说明

# mini-tc TODO

> 想法暂存区，先记录，暂不实现。

- [x] **Backspace 退回上一级目录** — 在文件列表中按 Backspace 键导航到父目录（含空目录修复）
- [x] **不支持预览格式时占位提示** — 若选中文件为不支持预览的格式（不在 `PREVIEWABLE_EXTENSIONS` 内），在另一侧面板直接展示「暂不支持预览该格式」，而不是静默无反应（涉及 `togglePreview` 及两处 `watch`）
- [x] **路径栏支持 Windows 占位符与 ~ 家目录** — 在 PathBar 的可编辑路径输入中支持展开：`%LOCALAPPDATA%\Netease` 等 `%VAR%` 环境变量，以及 `~` 代表当前用户根目录（`C:\Users\<user>`）。需在 `onEnter` 提交 `navigate` 前先做展开（建议后端或前端统一解析），否则 `pathExists` 会判定为不存在而报红
- [x] **支持鼠标返回键导航** — 部分鼠标侧键即「后退键」（`mousedown`/`mouseup` 中 `e.button === 3`，对应 XButton1）。在文件列表区域监听该键，触发与 Backspace 相同的「退回上一级目录」逻辑（注意区分左右面板，仅对当前活动面板生效）
- [x] **文件名模糊搜索** — 在某一栏直接输入文字时，默认按该文字对当前目录下的文件/文件夹名做模糊匹配并实时过滤列表（而非触发其他快捷键）。需处理：输入态与导航快捷键的冲突（例如仅在未聚焦输入框时启用，或提供独立搜索框）、区分大小写/子目录递归与否、清空即恢复全部列表
- [x] **支持 ctrl+c / ctrl+v / ctrl+x 剪贴板操作** — 在文件列表中选中条目后，可通过快捷键执行复制（ctrl+c）、粘贴（ctrl+v）、剪切（ctrl+x）。复制/剪切将当前选中项写入内存剪贴板（记录源路径+操作类型），粘贴时在**当前活动面板（用户当前选中的目录）**目录执行拷贝/移动。已在 App.vue 全局 keydown 拦截并 preventDefault（输入框焦点时放行，文本编辑正常）；粘贴后目标面板+活动面板都 refresh；复制保留剪贴板可重复粘贴，剪切粘贴后清空；后端 `copy_items`/`move_items` 递归处理目录，跨卷 move 用 copy+delete 兜底；目标已存在/源不存在则跳过并聚合错误，前端用 toast 反馈成功/失败
- [x] **Ctrl/Shift 多选文件** — 在文件列表中支持 `Ctrl`+点击 进行不连续多选、`Shift`+点击 进行连续范围多选，从而可一次性选中多个文件/文件夹，配合上述 ctrl+c / ctrl+x / ctrl+v 进行批量拷贝/剪切与粘贴。需处理：选中态的视觉高亮、活动面板 vs 另一侧面板的选中隔离、与模糊搜索过滤态共存、剪切态（移动）对多项的标记、全选/取消等便捷操作
  - 实现：`FileList.vue` 选中模型改为 `selectedIndices`(Set)+`activeIndex`+`anchorIndex`；`Ctrl+点击`切换、`Shift+点击`连续范围、`Ctrl+A`全选、`Esc`/点空白取消；每面板独立实例天然隔离左右选中；与 `/` 过滤态共存（索引基于 displayedEntries）；`App.vue` 的 ctrl+c/x/v 改为读取整个 `selectedEntries` 集合（后端 `copy_items`/`move_items` 本就支持多路径）；`Ctrl+X` 时通过 `cutNames` 把源面板中的待移动项以 `is-cut` 半透明斜体标记，粘贴后清除；`Delete` 支持批量删除。
- [x] **与操作系统剪贴板打通** — 当前的 ctrl+c / ctrl+x / ctrl+v 已与系统剪贴板双向互通（原仅走内部内存剪贴板）。拷贝/剪切时写入操作系统剪贴板（Windows 的 CF_HDROP，使资源管理器及其他程序可识别这些文件；或从外部复制文件进来时也能识别）；粘贴时既能消费内部记录，也能消费来自系统剪贴板的文件列表。处理了跨平台差异、权限，以及外部→内部、内部→外部两种方向的读写。
  - 实现：后端新增 `set_clipboard_files` / `get_clipboard_files` 命令。Windows 用原生 Win32 API（user32/kernel32/shell32）写/读 `CF_HDROP`：复制/剪切写入文件列表 + Unicode 文本副本，剪切额外写 `Preferred DropEffect=DROPEFFECT_MOVE`；粘贴读 `CF_HDROP`，由 DropEffect 决定移动/复制（无标记则复制），无 CF_HDROP 时回退解析 Unicode 文本中的真实路径。非 Windows 后端降级为 no-op，前端用 `navigator.clipboard` 文本回退（仅把含路径分隔符的行当文件）。前端 `App.vue`：复制/剪切时同步 `set_clipboard_files`；`pasteFromClipboard` 抽成共享 `doPaste(operation, sources)`，内部记录优先、为空则回退系统剪贴板。仅 `cargo check` 验证后端语法通过（前端改动未跑构建，待用户双击 `dev.bat` 实测）。
- [x] **地址栏面包屑 + 点击进入编辑态** — 当前 `src/components/PathBar.vue` 的地址栏**一直是可编辑 `<input>`**（见 line 15-27），缺少面包屑形态。目标改成：① **默认以面包屑展示**完整路径（按分隔符 `\\`/`/` 拆成各层目录段，`C:\Users\simon` → `C:` › `Users` › `simon`），每段为可点击元素；② **点击任意父目录段即可 `navigate` 回跳到该层**（如点 `Users` 跳到 `C:\Users`）；③ **仅当用户主动点击「编辑」时才切换到文本输入态**（保留现有 `onEnter`/`expandPath`/`pathExists` 校验逻辑）——可形式：点面包屑区右侧的 ✎/🖊 按钮、或双击面包屑空白区进入编辑，进入后复用现有 input 行为（聚焦全选、回车校验导航、Esc/失焦还原、非法路径标红 `invalid`）。需处理：驱动器前缀（如 `C:`）在面包屑里作为首段且可点；`~`/`%VAR%` 等展开态在面包屑里展示为展开后的真实路径段；面包屑过长时的省略/横向滚动；当前已有的「复制路径」📋 与「刷新」↻ 按钮保留。注意与现有 `editing` ref、`displayValue` watch、`onFocus/onBlur/onEnter/onEscape` 的衔接，不要丢失 `%VAR%`/`~` 展开校验能力。
  - 实现：`PathBar.vue` 新增 `segments` computed 将路径拆成可点击面包屑段（支持盘符 `C:`、`\\server\share` UNC、相对/POSIX 路径；末段 path 固定为原始 path 规避尾斜杠差异）；默认渲染面包屑，点击父段 `emit('navigate')` 回跳；新增 ✎ 按钮与双击面包屑进入编辑态（`startEdit` 复用聚焦全选），编辑态保留原 `onEnter`/`expandPath`/`pathExists` 校验与 `invalid` 标红；过长面包屑横向滚动。前端改动未跑构建，待用户双击 `dev.bat` 实测。

- [x] **支持 log 文件的查看** — 当前 `.log` 文件既不在前端 `PREVIEWABLE_EXTENSIONS`（`src/App.vue` line 344，含 txt/md/json/图片），也不在后端 `TEXT_EXTENSIONS`（`src-tauri/src/lib.rs` line 299，仅 `["TXT","MD","JSON"]`），所以选中 `.log` 会显示"暂不支持预览该格式"占位。目标：让 `.log` 像文本一样可预览。需改动：① 前端 `PREVIEWABLE_EXTENSIONS` 增加 `"log"`；② 后端 `TEXT_EXTENSIONS` 增加 `"LOG"`，使其经 `read_file_preview` 走 `text` 分支（受 `MAX_TEXT_SIZE`=2MB 上限约束）；③ 可选：`FilePreview.vue` 给 log 一个独立类型徽标/图标（现为通用 "TEXT"）；④ 注意常见 log 文件往往很大（>2MB），当前超限会报 "File too large to preview"，可考虑对 log 放宽上限或改为只读尾部 N 行（tail）而非全量，避免大日志卡顿/报错。实现参考第 16 条 JSON 预览（同样的 `PREVIEWABLE_EXTENSIONS` + `TEXT_EXTENSIONS` 双端放行模式）。
  - 实现：后端 `TEXT_EXTENSIONS` 加 `"LOG"`；超大 log（>2MB）改为 tail 末尾 512KB（`LOG_TAIL_LIMIT`，`seek`+`read_to_end`+丢弃首行残缺+顶部 banner 提示），txt/md/json 仍保持 2MB 上限报错；需 `use std::io::Seek`。前端 `App.vue` `PREVIEWABLE_EXTENSIONS` 加 `"log"`（`togglePreview`+两处 `watch` 自动豁免占位）。`FilePreview.vue`：log 走文本区、`ext==="log"` 时 `previewType="log"`、独立徽标 `LOG`(图标 📜)、footer 行数统计覆盖 log。`cargo check` 通过（exit 0），前端未跑构建，待用户双击 `dev.bat` 实测。

- [x] **预览文本文件时支持选中文本复制** — 当前 `FilePreview.vue` 的文本预览区（`<pre class="preview-text">`，约 line 35）是只读渲染，鼠标拖选文本后无法便捷复制：`App.vue` 全局 keydown 拦截了 `Ctrl+C` 走**文件剪贴板**，不会复制选中的预览文本；且 `<pre>`/`.text-body` 的可选性需确认未被 `user-select: none` 之类关掉。目标：预览 **文本类型** 文件（txt / json / log / md 等走后端 `read_file_preview` text 分支的）时，用户可在预览区内拖选文本并复制。需处理：① 确保文本预览区 `user-select: text` 可正常选中；② `Ctrl+C` 在预览区且存在文本选区时，走 `navigator.clipboard.writeText(selection)`（或 `document.execCommand('copy')`）复制选中文本，而非触发文件复制——即 `App.vue` 的全局 `Ctrl+C` 需判断「当前焦点/选区是否在预览文本区」，是则放行或主动复制选区；③ 可选：右键菜单「复制选中」、预览区「复制全部」按钮；④ 与现有「文件剪贴板」`Ctrl+C` 互不冲突（文件列表选文件→复制文件，预览区选文本→复制文本），注意区分「文件选中」与「文本选区」两种语义。
  - 实现：① `FilePreview.vue` 的 `.preview-text` 显式加 `user-select: text`；② `App.vue` 新增 `hasPreviewTextSelection()`（用 `window.getSelection()` 判断选区非空且锚点在 `.preview-text` 内），全局 `Ctrl+C`/`Ctrl+X` 命中该选区时直接 `return` 放行原生复制，不再触发文件复制/剪切——文件列表选文件→复制文件、预览区选文本→复制文本两种语义互不冲突；③ 预览区 footer 新增「复制全部」按钮（`copyAll`，优先 `navigator.clipboard.writeText`、失败回退隐藏 textarea + `execCommand('copy')`，成功后按钮短暂显示「已复制 ✓」）；④ 右键菜单「复制选中」本就由浏览器原生上下文菜单在选区存在时提供，无需额外实现。前端改动未跑构建，待用户双击 `dev.bat` 实测。

- [x] **右键菜单调用 7-Zip 等外部压缩工具解压** — 在文件列表的右键上下文菜单中，针对压缩包（zip/rar/7z 等）增加「用 7-Zip 解压」「用其他压缩工具解压」入口：自动探测系统已安装的 7-Zip（`C:\Program Files\7-Zip\7z.exe` 或 PATH 中的 `7z`）或其他压缩软件，将选中压缩包路径作为命令行参数（如 `7z x "<path>" -o"<target>"` 解压到当前面板目录）调用外部程序完成解压。需处理：工具未安装时的提示、解压目标目录选择（当前面板目录 vs 新建同名文件夹）、跨平台探测差异（macOS/linux 用 `unzip`/`7z`），以及该右键菜单本身依赖的上下文菜单基础设施（见后续右键菜单条目）
  - 实现：新增通用 `ContextMenu.vue`（浮层 + 外部点击关闭 + 视口越界翻转）。`FileList.vue` 在行/空白处 `@contextmenu.prevent` 时 `emit('ctx-menu')`（带 entry + clientX/Y，并选中该行）。`FilePanel.vue` 挂载时 `get_archive_tools()` 探测已安装工具（Windows：7-Zip 标准路径 + PATH `7z`/`7za`、WinRAR；非 Windows：`7z`/`7za`/`unzip`），按 entry 类型构建菜单：目录/文件「打开」「复制路径」、压缩包按各工具生成「解压到当前文件夹」「解压到"<stem>"」（无工具时显示禁用项「未检测到 7-Zip 等压缩工具」）、空白处「刷新」。后端新增 `get_archive_tools` / `extract_archive`：`extract_archive(archive, target_dir, tool_exe, syntax, mode)` 按 `syntax`（`7z`/`unzip`/`winrar`）构建命令，`mode="to_folder"` 解压到以压缩包名为基准的新子目录；`7z` 用 `x "<archive>" -o"<target>" -y`（路径含空格靠单参数拼接规避）。解压成功后 toast + 刷新面板。前端改动未跑构建，待用户双击 `dev.bat` 实测：`cargo check` 已通过。

- [x] **JSON 格式文件预览与美化** — 在文件预览（Ctrl+Q 对面面板）中支持 `.json` 文件：读取后用 `JSON.parse` 解析，再以 2 空格缩进 `JSON.stringify(obj, null, 2)` 美化输出。可在 `PREVIEWABLE_EXTENSIONS` 增加 `json`，并在 `read_file_preview` / 预览组件里按 JSON 处理：解析失败则提示「JSON 格式错误」并展示原始文本；与现有 text 预览复用同一面板与滚动。需处理：大文件上限（参考 `MAX_TEXT_SIZE`）、嵌套层级高亮可选、以及 `unsupported` 占位逻辑对 `.json` 的豁免（应走美化预览而非占位提示）。
  - 实现：后端 `read_file_preview` 的 `TEXT_EXTENSIONS` 加入 `JSON`，使 `.json` 像 txt/md 一样以原始文本读出（受 `MAX_TEXT_SIZE`=2MB 上限约束），解析/美化交给前端；前端 `App.vue` 的 `PREVIEWABLE_EXTENSIONS` 增加 `json`（豁免 `unsupported` 占位）；`FilePreview.vue` 拿到文本后若扩展名为 `json`，用 `JSON.parse` + `JSON.stringify(obj, null, 2)` 美化，解析失败则非阻塞提示「JSON 格式错误，以下为原始文本」并回退展示原始文本，类型徽标显示 `JSON`、图标 `🔧`。仅 `cargo check` 验证后端语法通过（前端改动未跑构建，待用户双击 `dev.bat` 实测）。

- [x] **统一配置保存到 `~/.minitc`（取代 localStorage）** — 当前散落在 `localStorage` 的配置应统一迁移到 `~/.minitc` 目录，与已有的 `video-config.json` 归并：① `FilePanel.vue` 的 `mini-tc-tabs-left` / `mini-tc-tabs-right`（打开的 tab 列表 + 激活 tab id）；② `App.vue` 的 `mini-tc-theme`（主题）。后端 `load_video_config`/`save_video_config` 已用 `home_dir().join(".minitc")` 落地，可新增通用命令（如 `load_app_config` / `save_app_config` 整份读写，或按子项 `load_config(name)` / `save_config(name, json)`）；前端把各 `localStorage.getItem/setItem` 改为对应 `invoke`。需处理：异步加载（`FilePanel.onMounted` 原同步读 localStorage 后 fallback 建首页 tab，须改为 await 配置后再决定 fallback）、首次启动无配置时的默认值、`video` 配置并入同一体系且保持兼容、以及 localStorage 旧数据的迁移/清理。
  - 实现：后端新增通用命令 `load_config(name)` / `save_config(name, config)`，统一落到 `~/.minitc/<name>.json`（带 `..`/分隔符路径穿越防护）；原专用 `load_video_config`/`save_video_config` 删除，`VideoPreview.vue` 改用通用命令读写 `video-config`（文件仍是 `~/.minitc/video-config.json`，兼容旧数据）。前端 `FilePanel.vue`（tabs-left/tabs-right）与 `App.vue`（theme）全部从 `localStorage` 迁移到通用命令；`onMounted` 改为 `await loadConfig` 后再决定 fallback 建首页 tab；旧 `localStorage` 数据在首次启动时迁移写入新存储并清理。仅 `cargo check` 验证后端语法通过（前端改动未跑构建，待用户双击 `dev.bat` 实测）。

- [x] **视频播放完自动播放下一个** — 当前视频预览播放结束后不会自动续播。目标：视频播放完毕后，自动顺序播放当前目录下的下一个视频文件（按当前文件列表排序），如果当前目录下所有视频都已播完，则停止播放。需处理：① 监听 `<video>` 元素的 `ended` 事件；② 从当前面板的文件列表中获取排在当前播放文件之后的下一个视频文件（按当前排序顺序，需与 `FileList` 的排序逻辑一致，考虑目录/文件混合排序规则）；③ 若存在下一个视频则自动加载并播放（复用现有 `VideoPreview` 的播放逻辑）；④ 若已是最后一个视频或目录中无更多视频，则停止（不循环）；⑤ 可选：提供「顺序播放」开关，让用户控制是否启用自动续播。

- [x] **地址栏路径过长时优化显示（消除横向滚动条）** — 当前 `PathBar.vue` 的面包屑区域（`.breadcrumb`）使用 `white-space: nowrap` + `overflow-x: auto`，当路径层级深或某段目录/文件名过长时会出现横向滚动条，既不美观也影响操作。目标：给出更好的方案替代横向滚动，让长路径在有限宽度内优雅展示。可选方案：① **中间折叠**——始终显示首段（盘符/根）和末段（当前目录名），中间段超宽时用 `…` 折叠（点击 `…` 可展开中间段列表供跳转）；② **逐段省略**——每个 `crumb` 设 `max-width` + `text-overflow: ellipsis` 截断过长的单段名称（hover/title 显示完整名）；③ **末段优先**——容器 `direction: rtl` 让溢出从左侧截断，保证当前目录名始终可见；④ 组合方案：单段过长用 `…` 截断 + 整体过长时中间段折叠。需处理：折叠后各段仍可点击导航、`title` 悬浮提示完整路径、与现有面包屑点击/双击编辑/✎ 按钮等交互不冲突。

  - 实现：`PathBar.vue` 面包屑改为默认 `overflow: hidden` 消除横向滚动条；新增隐藏测量代理 `.crumbs-measure`（含全部段）与 `measure()`（ResizeObserver / window resize 触发），路径整体超宽时 `needsCollapse=true` 自动折叠为「首段 › … › 末段」，中间段隐藏；每段 `max-width:220px` + `text-overflow:ellipsis` 单段过长截断，`title` 悬浮显示全名；点击 `…` 切换 `expanded` 临时展开全部（此时 `overflow-x:auto` 可滚动查看），各段仍 `.crumb` 可点击导航；与双击编辑 / ✎ 按钮 / 复制路径等交互无冲突。前端改动未跑构建，待用户双击 `dev.bat` 实测。

- [x] **修复：切换窗口后文件选中状态丢失** — 当应用窗口失去焦点（切换到其他程序）再切回来时，之前在文件列表中选中的文件变为未选中状态。这应该是个 bug。可能原因：① Tauri 窗口 `focus`/`blur` 事件触发了某些重置逻辑，导致 `selectedIndices`/`activeIndex` 被清空；② 窗口失焦时 Vue 响应式状态未持久化或被 watch 误重置；③ `FileList.vue` 的选中状态可能依赖于 `isFocused`/`activePanel` 等焦点相关 ref，窗口级 blur 导致这些 ref 被清除从而连带清除了选中态。需排查 `App.vue` 中窗口焦点监听、`FilePanel.vue` 的 `activePanel` 逻辑、`FileList.vue` 的 `selectedIndices`/`activeIndex`/`anchorIndex` 在窗口 blur/focus 时的变化链路，确保选中状态在窗口切换后保持不变。

- [x] **支持 PDF 预览** — 当前 `.pdf` 不在预览支持范围，选中会显示「暂不支持预览该格式」占位。目标：在文件预览（Ctrl+Q 对面面板）中内联渲染 PDF。需改动：① 前端 `App.vue` 的 `PREVIEWABLE_EXTENSIONS` 增加 `"pdf"`（豁免 unsupported 占位，逻辑同 log/json）；② 前端 `FilePreview.vue` 新增 `previewType === 'pdf'` 分支——PDF **不走** IPC 文本读取，而是像图片那样用 `convertFileSrc(props.filePath)` 拿到本地文件 URL，再用 `<iframe :src="url">`（或 `<embed>`/`<object>`）内联渲染，依赖系统 webview 的 PDF 查看器（Windows WebView2 内置 PDF 查看器可用；macOS WKWebView 不支持内联 PDF 会触发下载，需降级提示）；③ `FilePreview.vue` 的 `loadPreview` 中 `IMAGE_EXTENSIONS` 判断之外另立 PDF 分支走 `convertFileSrc`，不要误进入 text 分支；④ 需确认 Tauri v2 的 asset scope / CSP 允许 `<iframe>` 加载 `asset://localhost/...` 协议、以及 `convertFileSrc` 对 `.pdf` 是否放行（参考图片的处理）；⑤ 可选更稳方案：引入 pdf.js（`pdfjs-dist` + Canvas/`<canvas>`）做跨平台渲染，需调整 CSP 允许 worker 与本地 blob，体积更大；⑥ header 徽标 `PDF`(图标 📕)、footer 显示页数/大小；⑦ 处理大 PDF 与加载中/加载失败占位，并注意 PDF iframe 聚焦时浏览器原生 Ctrl+C（复制 PDF 内文本）应放行，参考「预览文本文件时支持选中文本复制」里 `hasPreviewTextSelection` 对文本选区复制的放行逻辑。
  - 实现：后端零改动——PDF 不入 `TEXT_EXTENSIONS`，不走 `read_file_preview` text 分支；`tauri.conf.json` 的 `csp: null` + `assetProtocol.scope: ["**"]` 已放行 `asset://`，无需额外配置。前端 `App.vue`：`PREVIEWABLE_EXTENSIONS` 加 `"pdf"`（豁免 unsupported 占位）；新增 `isPdfIframeFocused()`（class + tagName + src 三重校验），全局 keydown 中 PDF iframe 聚焦时放行原生 Ctrl+C（Ctrl+X 不放行，PDF 只读无剪切语义）。`FilePreview.vue`：`loadPreview` 在图片分支之后、文本分支之前独立新增 PDF 分支，用 `convertFileSrc` 生成 `asset://` URL 交给 `<iframe>` 渲染（依赖 WebView2 内置 PDF 查看器）；`:key="props.filePath"` 强制切换文件时重建 iframe 避免残留；三态占位（iframe / `pdfLoadError` 加载失败 / `pdfSupported` 为 false 的平台降级）；徽标 `PDF`（📕）；footer 只显示文件大小，不显示页数。平台检测用 UA 启发式（含 `Edg/` 判为 WebView2 支持，纯 AppleWebKit 判为不支持走降级），因项目未安装 `@tauri-apps/plugin-os` 且约束零新增依赖；macOS/Linux 走降级提示。`npm run build` exit 0；项目无 type-check/lint/test 脚本故未执行；待用户双击 `dev.bat` 实测。

- [x] **支持 doc / docx 文件预览** — 当前 `.doc`/`.docx` 不在预览支持范围，选中会显示「暂不支持预览该格式」占位。目标：在文件预览（Ctrl+Q 对面面板）中内联渲染 Word 文档。需改动：① 前端 `App.vue` 的 `PREVIEWABLE_EXTENSIONS`（`src/App.vue:366`）增加 `"doc"`, `"docx"`（豁免 unsupported 占位）；② Word 文档（尤其 .docx）是 OOXML（zip + xml），浏览器/webview 无法直接渲染，需转成 HTML 或纯文本再显示，可选方案：
  - **方案 A（推荐，纯前端零后端改动）**：引入 `mammoth` npm 包，前端用 Tauri 读文件二进制为 `ArrayBuffer`（可让后端 `read_file_preview`/`lib.rs:333` 新增 `binary` 分支按扩展名返回字节，或独立 `read_docx` 命令返回 `Vec<u8>`/base64），再用 `mammoth.convertToHtml({ arrayBuffer })` 转 HTML 在预览区以 `v-html` 渲染；样式还原度限于基础排版（标题/列表/加粗/表格/图片），复杂样式会丢失，UI 需注明。`.doc` 旧二进制格式 mammoth 不支持。
  - **方案 B（后端 Rust）**：引入 `docx` crate 提取文本/转 HTML；但 `.doc` 旧二进制格式 Rust 生态支持差，基本无解，需降级提示「仅支持 .docx，请另存为 .docx 后再预览」。
  - **方案 C（统一转 PDF 复用预览）**：调用本机 LibreOffice 命令行 `soffice --headless --convert-to pdf` 把 doc/docx 转 PDF，复用「支持 PDF 预览」的 iframe 渲染链路；代价是依赖用户安装 LibreOffice，且转换有延迟。
  ③ 前端 `FilePreview.vue` 新增 `previewType === 'doc'/'docx'` 分支渲染 HTML（`<div class="preview-doc" v-html="...">` + 基础排版样式，务必 **禁用 scripts 防 XSS**）；④ 后端可按方案 A 扩展 `read_file_preview` 的返回，或新增独立命令返回字节；⑤ header 徽标 `DOC`/`DOCX`(图标 📘)、footer 显示字数/页数；⑥ `.doc` 旧格式优先提示「格式不支持，建议用 Word 另存为 .docx 后再预览」；⑦ 渲染的 HTML 内用户选中文本后 Ctrl+C 应放行原生复制，参考「预览文本文件时支持选中文本复制」的 `hasPreviewTextSelection` 放行逻辑；⑧ 大文件 / 转换失败需有占位提示。
  - 实现：采用方案 A（纯前端零后端改动）。新增 npm 依赖 `mammoth`(1.12.0)，前端 `import mammoth from "mammoth/mammoth.browser.js"`（自包含浏览器包，避免 Node 版 `fs`/`path` 导致 Vite 构建失败）；`.docx` 经 `convertFileSrc` 拿 `asset://` URL 后 `fetch` 取 `arrayBuffer`，`mammoth.convertToHtml` 转 HTML，再用 `DOMParser` 防腐（去 `script/style/iframe/object/embed/link/meta`、去 `on*` 事件属性、去 `javascript:` 链接）后 `v-html` 渲染（基础排版样式用 `:deep` 作用于 v-html 内容）；`.doc` 旧二进制格式 mammoth 不支持，显示友好提示「请用 Word / WPS 另存为 .docx 后再预览」；header 徽标 `DOCX`/`DOC`(📘)、footer 显示字数（字符数）；`App.vue` 的 `hasPreviewTextSelection` 扩展匹配 `.preview-doc`，预览区内选中文本后 Ctrl+C 放行原生复制；>25MB 大文件与转换失败走占位/错误提示。前端改动未跑构建，待用户双击 `dev.bat` 实测。

- [ ] **删除失败（需管理员权限）时让用户选择是否提权重试** — 当前删除链路为 `FilePanel.vue:onDelete` → `deleteToTrash(fullPath)` → 后端 `delete_to_trash`（`src-tauri/src/lib.rs:414`，走 `trash::delete` 进回收站）。删某些受保护目录/文件时会因权限不足（Windows `Access is denied` / `ERROR_ACCESS_DENIED`，需管理员）而失败，当前实现仅 `error.value = e` 并设置 toast，但 `onDelete` 仍会 `entries.value = entries.filter(...)` 把失败项从列表里**就地移除**（视觉上像删成功了，实际没删），属于 bug。目标：当后端返回「权限不足」类错误时，前端**不要**移除该项，而是弹确认框让用户选择「以管理员身份重试」或「取消」。需处理：① 后端 `delete_to_trash` 需区分「权限错误」与「其他错误」（如路径不存在/被占用），可返回结构化错误（含 `kind` 字段，如 `"permission_denied"`），或前端用消息关键字（"denied"/"permission"）粗判；② Windows 提权删除方案——非管理员进程无法原地提权，必须启动**独立提权子进程**：可选 (a) `runas` 调用一个内置的小帮助程序 / 提权版 `powershell -Command "Remove-Item -Force -Recurse '<path>'"`（用 `shell_execute` 的 `runas` 动词弹 UAC），(b) 通过 `schtasks` 注册一次性任务以 SYSTEM/管理员跑 `Remove-Item` 再删除任务；③ 后端新增命令如 `delete_with_admin(path)` / 由前端直接 `Command::new("powershell").arg("Start-Process ...")` 触发 UAC；④ 用户「取消」时保持项不动并提示「未删除」；「重试」成功后 toast + 真正移除列表项；⑤ 提权子进程的路径转义（含空格/特殊字符）与删除进回收站 vs 直接 `Remove-Item`（绕过回收站）的取舍；⑥ 与现有 toast / `error.value` 反馈链路衔接，避免误删假象。

- [x] **文件名排序支持「自然排序（数字优先按数值）」** — 当前按名称排序用 `FileList.vue:222` 的 `a.name.toLowerCase().localeCompare(b.name.toLowerCase())`，这是**纯字典序**（逐字符比较），导致 `1a.jpg / 10b.jpg / 2c.jpg` 排成 `1a, 10b, 2c`（因为字符 `'1'` `'1' '0'` < `'2'`，即 "10" 排在 "2" 前面）。目标：数字片段按**数值大小**排，上述三文件应排成 `1a, 2c, 10b`。用户意图是「优先识别文件名中排在前面的数字、并以数字大小排序」。需处理：① 最简方案——`localeCompare(a, b, undefined, { numeric: true, sensitivity: 'base' })`，浏览器原生即做数字感知比较（`1` < `2` < `10`），且需确认中文/大小写灵敏度；② 更可控方案——写「自然排序」比较器：把名称拆成 `[数字, 文本]` 交替片段，数字段 `parseInt` 后按数值比、文本段按字典序比（参考 Explorer / 多数文件管理器的行为），注意多段数字（`a1b2` vs `a10b3`）、前导零、以及无数字文件名退化为纯文本序；③ 与现有「目录优先于文件」逻辑（`col==="name"` 时 `a.is_dir` 优先）共存，不受自然排序影响；④ 仅作用于 `name` 列排序，`size`/`modified` 排序保持原样；⑤ 可选：排序函数提到 `FileList.vue` 顶部或抽成 `utils/sort.js` 便于复用与单测；⑥ `cargo check` 无影响（纯前端改动），改完前端未跑构建，待用户双击 `dev.bat` 实测。
  - 实现：`FileList.vue` 的 name 排序由纯字典序 `a.name.toLowerCase().localeCompare(...)` 改为 `a.name.localeCompare(b.name, undefined, { numeric: true, sensitivity: "base" })`：浏览器原生数字感知比较，数字串按数值排（`1a < 2c < 10b`），`base` 灵敏度替代原 `toLowerCase()` 做大小写/重音不敏感；与现有「目录优先」逻辑共存（仅作用于同类型间），`size`/`modified` 列不变。纯前端改动，`cargo check` 无影响，待用户双击 `dev.bat` 实测。

- [x] **修复：视频播完自动续播时未真正自动播放（仅切换预览、停在暂停态）** — 这是 #33「视频播放完自动播放下一个」的**回归/未真正接上**项。现状：`VideoPreview.vue:onEnded`（line 291）已正确算出下一个视频并发 `emit("open-next", {...})`；`App.vue:playNextVideo`（line 439）也正确把 `previewFilePath/Name/Bytes` 换成下一个，触发 `VideoPreview` 的 `watch(() => props.filePath)`（line 558）→ 调 `loadVideo()`。但 `loadVideo()`（line 526）只重设 `videoSrc = convertFileSrc(props.filePath)` 并 `playing.value = false`，**从不调用 `videoEl.value.play()`**，且没有 `autoplay` 属性，于是新视频加载完成却停在暂停帧（中央显示 ▶ 大播放钮），与用户反馈「预览展示的是下一条，但是并没有自动播放」完全吻合。目标：自动续播时应真正开始播放下一个。需处理：① 区分「自动续播」与「用户手动打开/↑↓切换」——后者应保持暂停态让用户点 ▶（现有行为）；② 实现：新增 `pendingAutoplay` ref，`onEnded` 在 `emit("open-next")` 前设 `pendingAutoplay = true`；`loadVideo()` 入参 `autostart` 或在 `onLoadedMetadata`（line 269）里判断 `pendingAutoplay`——新 `src` 异步加载完成才 fire metadata，此时 `videoEl.value.play().catch(()=>{})` 并清 `pendingAutoplay`；③ 注意 `loadVideo` 先 `videoSrc.value = ""` 再赋新值，watch 触发时机要等真实 onLoadedMetadata；④ 浏览器自动播放策略可能拦截带声播放（用户已在本会话点过播放通常放行），被拒则静默停在暂停态即可；⑤ 仅 `VideoPreview.vue` 改动，纯前端，待用户双击 `dev.bat` 实测。
  - 实现：`VideoPreview.vue` 新增 `pendingAutoplay` ref；`onEnded` 在 `emit("open-next")` 前设 `true`；`onLoadedMetadata` 检测到则 `videoEl.value.play().catch(()=>{})` 并清旗；`onError` 也清旗防泄漏。另补「文件列表选中跟随」：`FileList.vue` 新增 `selectName(name)`（按名单选+滚动），经 `FilePanel.vue` 的 `defineExpose` 转暴露；`App.vue:playNextVideo` 在换预览前把来源面板（previewPanel 的对侧）的选中项移到 `payload.name`。改完前端未跑构建，待用户双击 `dev.bat` 实测。
  - 续改（用户要求「视频顺序与列表一致、优先数字、不要自己排序」）：`VideoPreview.onEnded` 不再 `listDirectory` 后字典序重排目录，改为只 `emit("open-next", { name: 当前文件名 })`；`FileList.vue` 新增 `getNextVideoEntry(name)`（在 `displayedEntries` 当前顺序里找下一个视频、跳过非视频与目录），经 `FilePanel.vue` 暴露；`App.vue:playNextVideo` 据来源面板 `getNextVideoEntry(当前名)` 解析下一视频并换预览+选中。因 `sortedEntries` 已用 `localeCompare(...,{numeric:true})` 自然排序，列表本身即 `爱莉希雅 (1)→(2)→…→(10)`，视频顺序据此自动一致；且若用户按 size/modified 排序，视频续播也跟随该顺序。纯前端，待用户双击 `dev.bat` 实测。

- [x] **H.265/HEVC 视频「有声音没图像」自动引导系统播放器（方案①）** — 用户文件 `爱莉希雅 (2).MP4` 编码为 HVC1(=HEVC/H.265)+AAC；WebView2(Chromium) 不解码 HEVC 的 Web 内容，但能解 AAC，于是黑屏有声且不触发 `<video>` 的 `error` 事件，原 `externalFallback` 兜底（仅靠 `@error`）永远不生效。用户选方案①：检测到"视频轨解不出画面"就自动弹出"用系统播放器打开"。实现（`VideoPreview.vue`）：① `onLoadedMetadata` 若 `videoWidth/videoHeight===0`（无可用视频轨）立刻 `triggerVideoFallback`；② 新增 `startVideoFrameWatch()` 在 `onPlay` 时启动：以 `requestVideoFrameCallback`（每帧上屏回调，设 `frameReceived`）为主信号，2s 后若仍无帧且 `currentTime>0.05`（声音在放）则用 canvas 采样——未解码视频画到 canvas 是**全透明**(alpha=0)、真实黑帧是**不透明**(alpha=255)，据此区分，确认无帧则 `triggerVideoFallback`；③ `cancelFrameWatch()` 在 `loadVideo`/`onBeforeUnmount` 清理定时器与回调，避免泄漏/误判；④ `triggerVideoFallback` 置 `externalFallback=true`+`loadError` 文案。正常 H.264 首帧几百 ms 内上屏不会误伤。纯前端改动，未跑构建，待用户双击 `dev.bat` 实测（重点：H.265 文件应在约 2s 内自动显示"用系统播放器打开"，普通 H.264 视频不受影响）。


